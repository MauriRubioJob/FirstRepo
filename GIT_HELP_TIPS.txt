Git install

git init    // To init on local
git clone url    // To clone from a repository on an URL
git add .    // add change of all the folders to my git
git commit    // Commit the changes localy
git push    // Push changes to repo over server

git pull   7Retireve changes

git tag

Download and install tortoisegit for windows shortcuts
Download and install Gitkraken for commit changes

git checkout v1.0   // To check previous version
git checkout main   // To go back to main

git revert number_of_commit 